#check_file_suffix
#Script snipplet that checks the suffix of a file
#it can be used for programming a file check integrety based on the file 
suffix

FILE=$1

if [ ${FILE##*.} != "suf" ]
then
ERROR
else
ACTION
fi

#better way may be using the magic number of a file