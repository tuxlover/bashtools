#!/bin/bash
CURRENT_DIR=$1

echo "Content_:"
echo "--------------------------------------------------------------"
for dir in $CURRENT_DIR
	do
		ls -lisah $dir
	done
echo "--------------------------------------------------------------"
echo "Content^:"

cd $CURRENT_DIR

exit 0
